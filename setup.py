from setuptools import setup, find_packages

setup(
    name="easy_thumbnails_webp",
    version="0.1",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[
        "django>=2.2",

    ],
)
